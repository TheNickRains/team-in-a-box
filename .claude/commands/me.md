---
description: Show my to-do list
---

Run: `git kb show {{KB_HUMAN_TODO_SLUG}}`

Output ONLY the bullets (everything under the `# {{HUMAN_NAME}}'s To-Do` heading). Strip the frontmatter. Strip the heading itself.

Do NOT add commentary, suggestions, staleness checks, or "want me to add anything?" prompts. Just the list. Silent otherwise.
